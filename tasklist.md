알겠습니다. 깃허브 `ahns456/calcul` 저장소의 설계문서를 바탕으로 전체 기능을 대상으로 하며, 형상관리가 용이하도록 PR 단위로 세분화된 할 일 목록을 생성하겠습니다. 프론트엔드, 백엔드, 테스트 작업을 모두 포함하여 설계 목표 달성에 초점을 맞출게요.

잠시만 기다려 주세요. 분석이 완료되면 바로 공유드리겠습니다.

## Feature Development Task List (Material You Calculator)

프로젝트 설계문서를 기반으로 **Material You Calculator** 앱 개발에 필요한 기능별 작업을 PR 단위로 세분화했습니다. 각 작업 항목은 구현 목표와 범위를 설명하며, **프론트엔드[FE]**, **백엔드[BE]**, **테스트[Test]** 태그로 구분했고 필요시 선행 의존 작업을 명시했습니다.

| ID  | 작업 (구현 목표)                            | 범위 / 상세 설명                                                                                                                                         | 태그                | 선행 작업    |
|-----|----------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------|-------------------|-----------|
| 1   | **프로젝트 초기 설정 및 아키텍처 구성**         | Flutter 프로젝트를 생성하고 **Material 3 디자인 시스템**을 적용한 기본 테마를 설정합니다. 설계서의 클린 아키텍처 구조에 따라 `lib/` 디렉토리를 `presentation/`, `application/`, `domain/`, `data/` 등으로 분리하고, 상태 관리(Riverpod)와 로컬 DB(Hive) 패키지를 추가하여 기반을 마련합니다. 초기 화면(예: 빈 Calculator 스캐폴드)을 구성하고 CI 설정(예: GitHub Actions 워크플로) 및 기본 테스트를 추가합니다. | [BE]             | (없음)      |
| 2   | **기본 UI 프레임 구축** (메인 계산기 화면)       | **원핸드 최적화 UI** 설계를 반영하여, 화면 상단에는 수식 및 결과 표시 영역을, 하단에는 그리드 형태의 버튼 배열을 구현합니다. Flutter Material3 위젯(`FilledButton` 등)을 활용하고 전체 앱 테마에 Material You 스타일을 적용합니다. 숫자 및 사칙연산 기호(+ − × ÷) 버튼을 비롯해 괄호, 소수점, ± 등 **모든 필수 입력 버튼**을 배치하며, 각 버튼은 누르면 화면에 해당 문자가 표시되도록 기본 이벤트 핸들러를 연결합니다 (계산 로직은 후속 작업에서 연동). UI는 반응형으로 설계하여 작은 화면에서는 단일 열, 큰 화면에서는 이중 패널 배치를 준비합니다. | [FE]             | 1         |
| 3   | **사칙연산 계산 로직 구현** (도메인 계층)       | 기본 **사칙연산(+ − × ÷)** 수식을 처리하는 도메인 로직(use case)을 구현합니다. 사용자 입력 수식을 파싱하여 연산을 수행하고, 연산 우선순위를 적용해 올바른 결과를 계산합니다. 계산 결과를 즉시 반환할 수 있도록 설계하여 **실시간 결과 미리보기** 기능에 활용합니다. 이 모듈은 UI와 독립적으로 동작하므로, 다양한 입력 시나리오(예: `2+2=4`, `2+3×4=14` 등)에 대한 **단위 테스트**를 작성하여 정확성을 검증합니다. | [BE][Test]      | 1         |
| 4   | **상태관리 연동 및 실시간 결과 표시**          | 도메인 계산 로직을 UI에 연결하기 위해 **상태 관리(Riverpod)**를 적용합니다. 계산기 입력 상태(현재 수식, 결과 미리보기)를 갖는 Provider를 정의하고, UI의 버튼 입력 이벤트마다 상태를 갱신하여 도메인 로직으로부터 **실시간 계산 결과**를 받아 표시합니다. 또한 잘못된 수식 입력 시 앱이 중단되지 않도록 예외 처리를 하고, 상태관리 로직에 대한 기본 **유닛 테스트** (예: 간단한 수식 입력 시 상태 변화 검증)를 추가합니다. | [FE][BE]        | 2, 3      |
| 5   | **확장 입력 기능 추가** (괄호, 소수점, ± 지원)  | 계산 로직을 확장하여 **괄호**, **소수점(decimal)**, **부호 토글(±)**을 지원합니다. 수식 파서에 괄호 계층을 처리하는 기능을 추가하고, 소수점 연산 및 음수 값 계산이 가능하도록 로직을 개선합니다. UI에 괄호 및 ± 입력 버튼이 없었던 경우 추가하고, 이미 배치된 해당 버튼들과 연동하여 작동시키며, 입력 가능한 형식을 제한하거나 잘못된 사용(예: 소수점 중복 입력 등)을 방지하는 검증 로직도 포함합니다. 새로운 연산 지원에 맞춰 **단위 테스트** 케이스를 보강하고, 괄호 중첩 계산, 소수 계산, 음수 계산 등의 시나리오에 대한 정확성을 검증합니다. | [FE][BE][Test] | 3, 4      |
| 6   | **계산 기록(History) 기능 구현**            | 사용자의 모든 계산 결과를 **히스토리로 저장**하고 조회하는 기능을 추가합니다. **Hive DB**를 활용하여 로컬에 계산 기록(수식, 결과, 시각)을 영속화하고, 새로운 계산 완료 시 해당 기록을 DB에 추가하는 도메인 로직을 구현합니다. 별도의 **기록 화면**(또는 모달)을 만들어 저장된 내역을 리스트뷰로 표시하고, 각 기록 항목에는 이전 계산식을 다시 불러오는 기능을 제공할 수 있습니다. 리스트 아이템을 **스와이프로 삭제**하면 해당 기록을 제거하는 UI 상호작용을 구현하고, 삭제 동작이 데이터베이스에 반영되도록 합니다. 기록 저장 및 삭제 로직에 대한 **유닛 테스트**를 작성하고, 기록 화면 UI에 대한 **위젯 테스트**(예: 스와이프 삭제 동작 확인)를 통해 기능을 검증합니다. | [FE][BE][Test] | 3, 4      |
| 7   | **진동/소리 피드백 및 설정 토글**            | 키 패드 버튼을 눌렀을 때 **햅틱 진동**과 **버튼 소리** 피드백을 제공하는 기능을 구현합니다. Android 기기의 진동 API와 사운드 효과를 사용하여 각 입력 시 촉각/청각 피드백을 주도록 하고, 사용자 선호에 따라 이를 켜고 끌 수 있도록 **설정 토글**을 제공합니다. 새로운 **설정 화면**을 추가하거나 기존 설정 UI가 있다면 이에 진동 및 소리 On/Off 스위치를 배치합니다. 설정 상태는 앱 내에 영속적으로 저장되어 (예: SharedPreferences 또는 Hive 사용) 앱 재시작 후에도 유지됩니다. | [FE][BE]        | 2, 4      |
| 8   | **테마 선택 및 다이내믹 컬러 연동**           | 사용자에게 **테마 설정** 옵션(F‑5)을 제공하여 Light/Dark 및 **다이내믹 컬러** 모드를 선택할 수 있도록 구현합니다. 앱의 Theme 데이터를 수정하여 **Material You 다이내믹 색상**을 지원하고, Android 12+ 기기의 시스템 배경화면 팔레트에 따라 자동으로 테마가 적용되도록 합니다. 설정 화면에 테마 선택 스위치를 추가하여 **다크 모드**, **라이트 모드**, **동적 색상** 중 하나를 사용자 지정으로 선택할 수 있으며, 선택 즉시 앱 테마에 반영됩니다. 다이내믹 컬러가 지원되지 않는 기기에서는 기본 색상 팔레트를 폴백으로 사용합니다. | [FE][BE]        | 1, 7      |
| 9   | **접근성 개선** (시멘틱 라벨 적용)           | **접근성(A11y)** 요구사항을 충족하도록 앱 전반을 점검하고 개선합니다. 모든 UI 요소(버튼, 토글 등)에 대해 스크린 리더가 읽을 수 있는 **Semantics 레이블**을 추가하여 토크백 사용자가 정확한 기능을 이해하도록 합니다. 또한 터치 타겟이 권장 크기(48dp 이상)를 만족하는지 확인하고 대비 비율도 충분한지 점검합니다. | [FE][Test]      | 2, 4      |
| 10  | **국제화(i18n) 지원**                    | 다국어 사용자를 위해 앱의 **국제화**를 구현합니다. Flutter의 로컬라이제이션 툴을 사용하여 영어 및 한국어 리소스 파일을 추가하고, UI의 모든 텍스트를 로컬라이즈하여 언어에 따라 표시되도록 합니다. 언어 변경 시 즉시 UI 텍스트가 변경되는지 확인하고, 주요 화면별로 **스냅샷 테스트** 등을 통해 다국어 문자열이 올바르게 표출되는지 검증합니다. | [FE][BE]        | 2         |
| 11  | **테스트 케이스 보강 및 성능 튜닝**          | 모든 핵심 로직에 대해 테스트 커버리지를 점검하고, **단위 테스트 커버리지 80%+** 목표를 달성하도록 부족한 테스트를 추가합니다. 특히 도메인 로직(계산 처리, 기록 관리)에 대한 경계값 테스트와, 주요 UI 컴포넌트에 대한 **위젯 테스트**를 작성합니다. 필요에 따라 **통합 테스트**(Flutter `integration_test`)를 도입하여 앱의 주요 사용 시나리오(계산 입력→결과→기록 저장 등)가 문제없이 동작하는지 검증합니다. 또한 비기능 요구사항에 맞춰 **퍼포먼스 튜닝**을 실시합니다. | [Test]          | 3-10      |
| 12  | **CI/CD 파이프라인 설정**                 | 저장소에 **CI 파이프라인**을 구성하여 매 PR마다 빌드와 테스트가 자동 실행되도록 합니다. GitHub Actions 워크플로를 추가해 **Flutter 테스트 + 커버리지**를 수행하고 결과를 지속적으로 모니터링합니다. 테스트 성공 및 정적 분석 통과 시에만 머지되도록 하고, Codecov 연동을 통해 커버리지 80% 미만일 경우 경고 또는 실패 처리하여 **품질 게이트**로 활용합니다. 또한 **CD 설정**으로 Fastlane을 사용해 빌드/배포 자동화를 준비합니다. | [BE][Test]      | 1         |
| 13  | **릴리스 준비 및 배포**                   | 모든 기능 구현 및 테스트가 완료되면, **베타 릴리스 배포** 및 최종 **스토어 출시 준비**를 진행합니다. 앱의 버전넘버를 올바르게 지정하고 **앱 아이콘**과 **스플래시 스크린**을 제작하여 적용합니다. Play 스토어 출시를 위해 필요한 앱 번들(`.aab`)을 생성하고, Fastlane으로 메타데이터(앱 이름, 설명, 스크린샷)를 함께 준비합니다. Firebase App Distribution을 통해 테스터들에게 베타 버전을 배포하고 피드백을 반영한 후, Google

